# 工作流

工作流用于将多条命令按顺序组合执行。

## 管理

在 **选项 → 工作流** 中可新增、编辑、删除工作流。每个工作流包含多行，每行格式为：

- `输入 | n`：执行该输入对应的命令，并选中第 `n` 个结果（从 0 开始）执行。
- `输入 | 1-n`：对第 1 到第 n 个结果依次执行。
- `输入 | all`：对该命令下的所有结果依次执行。

## 执行

在命令框中输入触发词 `wf`，可列出已保存的工作流；选中某一工作流并回车，即按上述规则顺序执行其中每一行。

执行过程中可根据需要配合修饰键（如 ⌥ 当前标签、⇧ 批量打开）使用。
